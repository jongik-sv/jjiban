# TSK-01-02: 터미널 UI 컴포넌트 - 화면설계

## 문서 정보

| 항목 | 내용 |
|------|------|
| Task ID | TSK-01-02 |
| 문서 버전 | 1.0 |
| 작성일 | 2025-12-17 |
| 상태 | 화면설계 완료 |

---

## 1. 화면 구성 개요

### 1.1 컴포넌트 배치도

```
┌─────────────────────────────────────────────────────────────────────────────┐
│ AppHeader.vue                                                               │
│ ┌─────┐ ┌──────────────┐                    ┌─────────────────────────────┐ │
│ │ 로고 │ │ 네비게이션   │                    │ [🔔] [👤] [📺 TerminalIcon] │ │
│ └─────┘ └──────────────┘                    └─────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 1.2 터미널 다이얼로그 레이아웃

```
┌────────────────────────────────────────────────────────────────────────────┐
│ 터미널 세션 관리                                         [□][▢][✕]        │
├────────────────────────────────────────────────────────────────────────────┤
│ ┌───────────────────────┐ ┌──────────────────────────────────────────────┐ │
│ │ 세션 목록 (250px)     │ │ 터미널 뷰 (flex-1)                           │ │
│ │ ┌───────────────────┐ │ │                                              │ │
│ │ │ ● TSK-01-01       │ │ │ ┌──────────────────────────────────────────┐ │ │
│ │ │   [build] 실행중  │ │ │ │ $ /wf:build TSK-01-01                    │ │ │
│ │ │ ──────────────────│ │ │ │ [wf:build] Task 빌드 시작...             │ │ │
│ │ │ ○ TSK-02-01       │ │ │ │ > 소스 컴파일 중...                      │ │ │
│ │ │   [done] 완료     │ │ │ │ > 테스트 실행 중...                      │ │ │
│ │ │ ──────────────────│ │ │ │ ✓ 빌드 완료                              │ │ │
│ │ │ ○ Session #3      │ │ │ │ $                                        │ │ │
│ │ │   대기 중         │ │ │ │                                          │ │ │
│ │ └───────────────────┘ │ │ └──────────────────────────────────────────┘ │ │
│ │ ┌───────────────────┐ │ │                                              │ │
│ │ │ + 새 세션         │ │ │                                              │ │
│ │ └───────────────────┘ │ │                                              │ │
│ └───────────────────────┘ └──────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────────────────────┘
```

---

## 2. 컴포넌트 상세 설계

### 2.1 TerminalHeaderIcon.vue

#### 2.1.1 와이어프레임

```
┌───────────────────────────────┐
│ 일반 상태     │ 활성 세션    │
│               │              │
│   ┌─────┐     │   ┌─────┐    │
│   │ 📺  │     │   │ 📺  │ ⓷  │
│   └─────┘     │   └─────┘    │
│               │  (배지: 3)   │
└───────────────────────────────┘
```

#### 2.1.2 상세 스펙

| 요소 | 스펙 | 클래스/스타일 |
|------|------|--------------|
| 컨테이너 | 40x40px, cursor: pointer | `terminal-header-icon` |
| 아이콘 | pi-desktop, 20px | `text-surface-600 hover:text-primary-500` |
| 배지 | 우상단, min-width 18px | `p-badge p-badge-primary` |
| 호버 | 배경 surface-100 | `hover:bg-surface-100 rounded` |
| 트랜지션 | 150ms ease | `transition-colors duration-150` |

#### 2.1.3 상태별 표시

| 상태 | 아이콘 색상 | 배지 |
|------|------------|------|
| 기본 (세션 0) | surface-600 | 숨김 |
| 활성 세션 있음 | primary-500 | 세션 수 표시 |
| 호버 | primary-600 | - |

---

### 2.2 TerminalDialog.vue

#### 2.2.1 다이얼로그 사양

| 속성 | 값 | 설명 |
|------|-----|------|
| width | 90vw | 뷰포트 90% |
| height | 80vh | 뷰포트 80% |
| min-width | 800px | 최소 너비 |
| min-height | 500px | 최소 높이 |
| modal | true | 모달 오버레이 |
| maximizable | true | 최대화 버튼 |
| draggable | true | 드래그 이동 |
| closeOnEscape | true | ESC 닫기 |

#### 2.2.2 내부 레이아웃

```
┌────────────────────────────────────────────────────────────────┐
│ [content 영역]                                                 │
│ ┌─────────────────┬────────────────────────────────────────────┐
│ │                 │                                            │
│ │ SessionList     │ TerminalView                               │
│ │ (250px fixed)   │ (flex: 1)                                  │
│ │                 │                                            │
│ │                 │                                            │
│ └─────────────────┴────────────────────────────────────────────┘
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

| 영역 | 너비 | 클래스 |
|------|------|--------|
| 전체 컨테이너 | 100% | `flex h-full` |
| 세션 목록 패널 | 250px (고정) | `w-[250px] border-r border-surface-200` |
| 터미널 뷰 패널 | flex-1 | `flex-1 min-w-0` |

---

### 2.3 TerminalSessionList.vue

#### 2.3.1 세션 항목 레이아웃

```
┌─────────────────────────────────────┐
│ ┌─────────────────────────────────┐ │
│ │ ● TSK-01-01               [✕]  │ │  ← 선택된 항목 (bg-surface-100)
│ │   [build] 실행중               │ │
│ └─────────────────────────────────┘ │
│ ┌─────────────────────────────────┐ │
│ │ ○ TSK-02-01               [✕]  │ │  ← 일반 항목
│ │   [done] 완료                   │ │
│ └─────────────────────────────────┘ │
│ ┌─────────────────────────────────┐ │
│ │ ○ Session #3               [✕]  │ │  ← Task 없는 세션
│ │   대기 중                       │ │
│ └─────────────────────────────────┘ │
│ ─────────────────────────────────── │
│ ┌─────────────────────────────────┐ │
│ │     + 새 세션                   │ │  ← 생성 버튼
│ └─────────────────────────────────┘ │
└─────────────────────────────────────┘
```

#### 2.3.2 세션 항목 상세

| 요소 | 스펙 | 클래스 |
|------|------|--------|
| 컨테이너 | padding 12px | `p-3 cursor-pointer` |
| 선택 상태 | bg-surface-100 | `bg-surface-100` |
| 호버 | bg-surface-50 | `hover:bg-surface-50` |
| 상태 아이콘 | 8px 원형 | (아래 참조) |
| Task ID | 14px, semibold | `text-sm font-semibold text-surface-900` |
| 닫기 버튼 | 24x24px | `pi-times text-surface-400 hover:text-red-500` |
| 명령어 | 12px, surface-500 | `text-xs text-surface-500` |

#### 2.3.3 상태 아이콘

| 상태 | 아이콘 | 색상 클래스 |
|------|--------|------------|
| running | ● (filled circle) | `text-green-500` |
| completed | ○ (outline circle) | `text-surface-400` |
| error | ○ (outline circle) | `text-red-500` |
| connecting | 🔄 (spinner) | `text-blue-500 animate-spin` |

#### 2.3.4 새 세션 버튼

| 요소 | 스펙 |
|------|------|
| 높이 | 44px |
| 배경 | transparent → hover:surface-50 |
| 아이콘 | pi-plus (surface-500) |
| 텍스트 | "새 세션" (surface-600) |
| 정렬 | center |

---

### 2.4 TerminalView.vue

#### 2.4.1 터미널 테마

```typescript
// Catppuccin Mocha 기반 테마
const terminalTheme = {
  background: '#1e1e2e',      // 배경
  foreground: '#cdd6f4',      // 기본 텍스트
  cursor: '#f5e0dc',          // 커서
  cursorAccent: '#1e1e2e',    // 커서 내부
  selectionBackground: '#585b70',  // 선택 영역
  selectionForeground: '#cdd6f4',

  // ANSI 색상
  black: '#45475a',
  red: '#f38ba8',
  green: '#a6e3a1',
  yellow: '#f9e2af',
  blue: '#89b4fa',
  magenta: '#f5c2e7',
  cyan: '#94e2d5',
  white: '#bac2de',

  brightBlack: '#585b70',
  brightRed: '#f38ba8',
  brightGreen: '#a6e3a1',
  brightYellow: '#f9e2af',
  brightBlue: '#89b4fa',
  brightMagenta: '#f5c2e7',
  brightCyan: '#94e2d5',
  brightWhite: '#a6adc8'
}
```

#### 2.4.2 터미널 옵션

| 옵션 | 값 | 설명 |
|------|-----|------|
| fontSize | 14 | 폰트 크기 |
| fontFamily | 'Consolas, "Courier New", monospace' | 폰트 |
| lineHeight | 1.2 | 줄 높이 |
| cursorBlink | true | 커서 깜빡임 |
| cursorStyle | 'block' | 커서 스타일 |
| scrollback | 10000 | 스크롤백 라인 |
| convertEol | true | EOL 변환 |
| allowProposedApi | true | 제안 API 허용 |

#### 2.4.3 터미널 컨테이너

| 요소 | 스펙 |
|------|------|
| 컨테이너 | 100% width, 100% height |
| 패딩 | 8px (터미널 내부 여백) |
| border-radius | 0 (다이얼로그 내부) |
| 배경 | #1e1e2e (터미널 테마와 동일) |

#### 2.4.4 연결 상태 오버레이

```
┌──────────────────────────────────────────────────────────────────┐
│                                                                  │
│                                                                  │
│                    ┌───────────────────────┐                     │
│                    │  🔄 연결 중...        │                     │
│                    └───────────────────────┘                     │
│                                                                  │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘

또는 (에러 시):

┌──────────────────────────────────────────────────────────────────┐
│                                                                  │
│                    ┌───────────────────────┐                     │
│                    │  ⚠️ 연결 실패          │                     │
│                    │  [재연결]             │                     │
│                    └───────────────────────┘                     │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
```

---

## 3. 인터랙션 설계

### 3.1 터미널 열기 플로우

```
1. TerminalHeaderIcon 클릭
   └─ TerminalDialog.visible = true
      └─ 세션 목록 로드 (terminalStore.sessions)
         └─ 활성 세션 있음?
            ├─ Yes: 해당 세션의 TerminalView 표시
            └─ No: 빈 터미널 뷰 (+ 새 세션 안내)
```

### 3.2 세션 생성 플로우

```
1. "+ 새 세션" 클릭
   └─ terminalStore.createSession()
      └─ POST /api/terminal/session 호출
         └─ 성공: sessions에 추가, 자동 선택
         └─ 실패: 에러 토스트 표시
```

### 3.3 세션 전환 플로우

```
1. 세션 항목 클릭
   └─ terminalStore.setActiveSession(sessionId)
      └─ TerminalView 재렌더링
         └─ 새 세션의 SSE 연결
            └─ 기존 출력 버퍼 표시
```

### 3.4 세션 종료 플로우

```
1. 세션 [✕] 클릭
   └─ 확인 다이얼로그 (실행 중인 경우)
      └─ terminalStore.closeSession(sessionId)
         └─ DELETE /api/terminal/session/:id
            └─ 성공: sessions에서 제거
               └─ 다른 세션 자동 선택 (있는 경우)
```

---

## 4. 키보드 단축키

| 단축키 | 동작 | 컨텍스트 |
|--------|------|----------|
| Ctrl + ` | 터미널 다이얼로그 토글 | 전역 |
| Ctrl + Shift + N | 새 세션 생성 | 다이얼로그 내 |
| Ctrl + Tab | 다음 세션 | 다이얼로그 내 |
| Ctrl + Shift + Tab | 이전 세션 | 다이얼로그 내 |
| Ctrl + W | 현재 세션 닫기 | 다이얼로그 내 |
| Escape | 다이얼로그 닫기 | 다이얼로그 내 |
| Ctrl + + | 폰트 크기 증가 | 터미널 내 |
| Ctrl + - | 폰트 크기 감소 | 터미널 내 |

---

## 5. 반응형 설계

### 5.1 브레이크포인트

| 화면 크기 | 레이아웃 변경 |
|-----------|--------------|
| ≥ 1024px | 기본 레이아웃 (좌우 분할) |
| 768px ~ 1023px | 세션 목록 200px |
| < 768px | 세션 목록 탭 전환 방식 |

### 5.2 모바일 레이아웃 (< 768px)

```
┌────────────────────────────────────────────┐
│ 터미널 세션                          [✕]   │
├────────────────────────────────────────────┤
│ [세션 목록] [터미널]                        │  ← 탭
├────────────────────────────────────────────┤
│                                            │
│  (선택된 탭 내용)                          │
│                                            │
│                                            │
│                                            │
└────────────────────────────────────────────┘
```

---

## 6. 애니메이션 및 트랜지션

### 6.1 다이얼로그

| 요소 | 애니메이션 |
|------|-----------|
| 열기 | fade-in + scale(0.95 → 1), 200ms |
| 닫기 | fade-out + scale(1 → 0.95), 150ms |
| 오버레이 | fade-in/out, 200ms |

### 6.2 세션 목록

| 요소 | 애니메이션 |
|------|-----------|
| 항목 추가 | slide-in-left, 150ms |
| 항목 제거 | slide-out-left + fade-out, 150ms |
| 선택 전환 | background-color, 100ms |

### 6.3 터미널

| 요소 | 애니메이션 |
|------|-----------|
| 커서 | blink, 1s infinite |
| 연결 스피너 | rotate, 1s linear infinite |
| 출력 스크롤 | smooth scroll |

---

## 7. 접근성 (A11y)

### 7.1 ARIA 레이블

| 컴포넌트 | ARIA 속성 |
|----------|-----------|
| TerminalHeaderIcon | `aria-label="터미널 열기"`, `aria-haspopup="dialog"` |
| TerminalDialog | `role="dialog"`, `aria-labelledby="terminal-dialog-title"` |
| TerminalSessionList | `role="listbox"`, `aria-label="터미널 세션 목록"` |
| 세션 항목 | `role="option"`, `aria-selected` |
| TerminalView | `role="log"`, `aria-live="polite"` |

### 7.2 포커스 관리

1. 다이얼로그 열림 → 첫 번째 세션 항목에 포커스
2. 세션 닫힘 → 이전 세션 또는 "+ 새 세션"에 포커스
3. 다이얼로그 닫힘 → TerminalHeaderIcon에 포커스 복원

---

## 8. 에러 상태 UI

### 8.1 연결 실패

```
┌────────────────────────────────────────┐
│        ⚠️ 터미널 연결 실패             │
│                                        │
│   서버와 연결할 수 없습니다.           │
│   네트워크 상태를 확인해주세요.        │
│                                        │
│   [재연결]  [닫기]                     │
└────────────────────────────────────────┘
```

### 8.2 세션 종료됨

```
┌────────────────────────────────────────┐
│        ℹ️ 세션이 종료되었습니다        │
│                                        │
│   터미널 세션이 종료되었습니다.        │
│                                        │
│   [새 세션 시작]                       │
└────────────────────────────────────────┘
```

---

## 변경 이력

| 버전 | 날짜 | 변경 내용 |
|------|------|-----------|
| 1.0 | 2025-12-17 | 초안 작성 |
